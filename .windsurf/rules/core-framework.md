---
trigger: always_on
---

## Core Framework
- **Framework:** React 18+ with Vite (Latest version).
- **Language:** TypeScript (Strict mode enabled). 
- **State Management:** Use React Context API for global theme/user state. Use local state (`useState`, `useReducer`) for component-level logic. Avoid external state libraries unless complexity demands it.